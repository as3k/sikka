{{!< default}}
 <div class="gh-page">
  <div class="gh-container relative">
    <div
      class="bg-text-overlay absolute mt-36 font-playfair text-supersize lh-1 z-0 -ml-40 mt-32 text-nord4 opacity-25 select-none">
      LATEST</div>
    <div class="gh-postfeed relative z-10">
      {{#foreach posts}}

      {{> "card"}} {{!-- partials/card.hbs --}}

      {{/foreach}}
    </div>

  </div>
</div>